{
  "name": "banyone-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "@types/react": "19.2.0",
    "react-test-renderer": "19.2.4"
  },
  "dependencies": {
    "expo-router": "~55.0.7"
  },
  "scripts": {
    "lint": "npm run lint --workspace mobile && npm run lint --workspace backend && npm run lint --workspace @banyone/contracts",
    "typecheck": "npm run typecheck --workspace mobile && npm run typecheck --workspace backend && npm run typecheck --workspace @banyone/contracts",
    "test": "npm run test --workspace mobile && npm run test --workspace backend && npm run test --workspace @banyone/contracts"
  }
}
