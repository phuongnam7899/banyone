name: Contracts CI

on:
  push:
    paths:
      - "packages/contracts/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.base.json"
      - ".github/workflows/contract-ci.yml"
  pull_request:
    paths:
      - "packages/contracts/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.base.json"
      - ".github/workflows/contract-ci.yml"

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: npm run lint --workspace @banyone/contracts
      - run: npm run typecheck --workspace @banyone/contracts
      - run: npm run test --workspace @banyone/contracts
